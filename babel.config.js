module.exports = {
    presets: [
        "@babel/preset-env", // Include if needed
        "@babel/preset-react" // Include if needed
    ],
    plugins: [
        "@babel/plugin-proposal-private-property-in-object"
    ]
};